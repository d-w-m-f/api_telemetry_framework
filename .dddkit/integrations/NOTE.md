This note is context only, not part of the dddkit structure.

`dddkit.manifest.json` is now real: generated by `.dddkit/scripts/generate-manifest.py`
and checked by `validate-ddd.py`. `claude.manifest.json` is still a stub - it will
track the dddkit-specific SKILL.md files (`/interview`, `/map-contexts`, etc.) once
those exist; there is nothing to hash yet.
