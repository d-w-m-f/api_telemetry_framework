This note is context only, not part of the dddkit structure.

Both manifests are now real, generated by `.dddkit/scripts/generate-manifest.py`
(`--target dddkit` / `--target claude`) and checked by `validate-ddd.py`.
`claude.manifest.json` currently covers `interview`, `map-requirements`, and
`constitution` (plan/001-003); it grows as the remaining SDK skills
(`map-contexts`, `model-context`, `plan-context`, `generate-tasks`, `implement`,
`implement-progress`, `checklist`, `discover-bounded-context`) are implemented -
rerun `generate-manifest.py --target claude` after adding any of them.
