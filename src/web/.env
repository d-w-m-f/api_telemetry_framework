# Template, loaded by @ngx-env/builder (see angular.md §4). Copy to .env.local for local dev.
# .env.prod only loads when NG_APP_ENV=prod is set for the build (e.g. `NG_APP_ENV=prod npm run build`).

NG_APP_API_BASE_URL=http://localhost:8080
