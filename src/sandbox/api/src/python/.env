# Template. Copy to .env.local for local dev; .env.prod is filled in by deployment tooling. Never commit a
# real .env.local/.env.prod with live credentials.

ENVIRONMENT=local
DATABASE_URL=postgresql://sandbox:sandbox@sandbox-db:5432/sandbox
DB_DRIVER=asyncpg
APP_VARIANT=fastapi_async
PORT=8000
