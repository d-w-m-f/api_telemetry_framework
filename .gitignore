*.txt

.claudeignore/*
!.claudeignore/.keep

# Build artifacts (per-app tooling already covers most of this; these are the repo-wide catch-alls)
target/
__pycache__/
*.pyc
dist/
node_modules/

# mise / overmind local runtime artifacts -- never shared, per-contributor only
mise.local.toml
.overmind.env
.overmind.sock