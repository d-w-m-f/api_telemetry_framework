# Template. docker compose only auto-loads a file literally named .env next to the compose file, so select
# an environment explicitly: `docker compose --env-file docker/.env.local -f docker/docker-compose.yml up`.

MAINDB_USER=app
MAINDB_PASSWORD=app
MAINDB_NAME=maindb
